/**
 * Status constants
 * @type {{INITIALIZED: string, STARTED: string, STOPPED: string, PAUSED: string}}
 */
module.exports = {
  INITIALIZED: 'initialized',
  STARTED: 'started',
  STOPPED: 'stopped',
  PAUSED: 'paused',
};